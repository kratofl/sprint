/** Compatibility fill tokens retained for legacy gradient import names. */

/** Primary compatibility fill maps to flat racing orange (Orange/500). */
export const gradientAccent = '#FF6A00'

/** Subtle orange tint — hover states, active backgrounds (Primary/BG-Soft). */
export const gradientAccentSubtle = '#FF6A001A'

/** @deprecated Secondary CTA no longer defaults to cyan; maps to Surface/Panel. */
export const gradientTeal = '#141414'

/** @deprecated Cyan-first hover is inert; maps to Surface/Tile fill. */
export const gradientTealSubtle = '#1F1F1F'
